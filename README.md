# github-reports

Gets info from Github!

## Requirements

 * nodejs
 * npm
 * coffeescript
 
On a Mac g0t brew?
 `brew install node`
 `npm install -g coffee-script`

## Usage

 * `npm install`
 * `coffee github-reports.coffee`
